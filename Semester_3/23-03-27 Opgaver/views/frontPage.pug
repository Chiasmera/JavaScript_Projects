html
    head
        meta(char-set='UTF-8')
        title= links.title
        link(rel="stylesheet", href="css/styles.css")
    body
        each link in links
            #link
                a(href= link) #{link}